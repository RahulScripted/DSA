// Write a program to calculate the area of the rectangle.

#include<iostream>
using namespace std;
int main(){
    int length,breadth;
    cout<<"Enter the length : ";
    cin>>length;
    cout<<"Enter the breadth : ";
    cin>>breadth;
    cout<<"Area will be : "<<(length * breadth);
}